#include <string>
#include <vector>
#include <bits/stdc++.h>
#define ll long long
#define rep(i, l, r) for(int i = l; i<r; i++)
using namespace std;

vector<vector<int>> solution(string s, vector<vector<int>> grid){
    vector<vector<int>> answer;

    return answer;
}